{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd thapargpt-frontend && npm install && npm run build",
    "startCommand": "serve -s thapargpt-frontend/build -l 3000",
    "installCommand": "cd thapargpt-frontend && npm install",
    "nodeVersion": "18"
  }
}