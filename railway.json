{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd thapargpt-frontend && npm install && npm run build",
    "startCommand": "cd thapargpt-frontend && npm start",
    "nodeVersion": "16"  
  }
}